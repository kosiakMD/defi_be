FROM node:14.17 as development

# Install development dependencies
WORKDIR /usr/src/app
COPY package*.json ./
RUN yarn install --loglevel verbose

# Build application
COPY . .
RUN ls -l apps
RUN yarn account-build

# Configure application startup command
CMD ["npm", "run", "start"]



FROM node:14.17 as pre-production
# Install runtime dependencies
WORKDIR /usr/src/app
COPY package*.json ./
RUN yarn install --only=production

FROM node:14.17-alpine as production

# Setup default environment variables
ENV NODE_ENV production
# Copy runtime dependencies and application code
WORKDIR /usr/src/app
COPY --from=pre-production /usr/src/app/node_modules ./node_modules
#COPY --from=development /usr/src/app/apps/account_service/config/.env /dist/apps/account_service/.env
COPY --from=development /usr/src/app/apps/account_service/config/values.yaml /dist/apps/values.yaml
COPY --from=development /usr/src/app/dist ./dist
RUN touch /dist/apps/account_service/.env

# Configure application startup command
ENTRYPOINT ["node"]
CMD ["dist/apps/account_service/src/main"]
